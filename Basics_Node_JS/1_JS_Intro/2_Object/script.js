// obj={
//     name:"Navjot",
//     age:21,
//     key:"value"
// }
// Object.freeze(obj);
// obj.age = 25;//due to above function this will not affect the output even due to same obj.name
// console.log(obj);

var arr = [1,2,3,4,5,6];
function abcd(){
    // we can find the length of the function using abcd.length in the console
}