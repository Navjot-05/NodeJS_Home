1. Express js is a npm package

2. it's just a framework

3. it manages everything from recieving request to giving the response

//Setting up the basic express js application 

## basic syntax of the whole is ##

1. app.get(route,requestHandler)
2. app.listen(3000,function(){})

a. route = "/"
b. requestHandler = function(res,req){}

NOTE requesthandler is also a middleware as the route is 