# create user account 
    mongoose setup 
    schema
    model
    usercreate -> password -> hash
    jwt token -> that should be set as cookie in browser

# Login 
    recieves the token -> decrypt -> email